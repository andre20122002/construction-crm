BudSklad ERP

Система автоматизації закупівель та складського обліку для будівельних компаній.

🚀 Як запустити локально (Development)

Клонуйте репозиторій:

git clone <repo_url>
cd construction_crm


Створіть віртуальне оточення:

python -m venv venv
source venv/bin/activate  # Mac/Linux
venv\Scripts\activate     # Windows


Встановіть залежності:

pip install -r requirements.txt


Налаштуйте змінні оточення:
Створіть файл .env на основі прикладу:

cp .env.example .env


Відкрийте .env і впишіть пароль до вашої бази даних DB_PASSWORD.

Підготуйте базу даних:

python manage.py makemigrations
python manage.py migrate
python manage.py seed_data  # Наповнити демо-даними


Запустіть сервер:

python manage.py runserver


🌐 Як запустити "Пілот" (Production / Server)

У файлі .env змініть налаштування:

DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=ваша-доменна-адреса.com,IP-сервера
DJANGO_SECRET_KEY=<згенеруйте_новий_складний_ключ>
CSRF_TRUSTED_ORIGINS=[https://ваша-доменна-адреса.com](https://ваша-доменна-адреса.com)


Зберіть статику (CSS/JS):

python manage.py collectstatic --noinput


Запустіть через Gunicorn (професійний сервер):

gunicorn construction_crm.wsgi:application --bind 0.0.0.0:8000


🛠 Функціонал

Менеджер: Створення заявок, контроль бюджету (SAP-аналог), звіти.

Прораб: Мобільний інтерфейс, замовлення матеріалів, фото-звіт прийому.